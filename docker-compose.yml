services:
  # Base de datos SQLite se almacena localmente en server/prisma/dev.db
  # No se requiere servicio de contenedor para la base de datos persistente en modo local.


volumes:
  postgres_data: